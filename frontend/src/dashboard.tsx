import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "nuapp" />
      <CardContent> it is a registry of apps < /CardContent>
      </Card>
    );